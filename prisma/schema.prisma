generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id   String @id @default(uuid())
  name String
  cpf  String @unique

  // Credentials para o Auth.js
  email         String?   @unique // email principal (login)
  emailVerified DateTime?
  passwordHash  String? // hash para Credentials

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // 🔹 processos onde ele é o dono (quem abriu)
  processesOwned Process[]              @relation("ProcessOwner")
  // 🔹 e-mails do usuário (pode ter vários)
  emails         UserEmail[]
  // departamentos ao qual o usuário é membro
  memberships    DepartmentMembership[]

  // Auth.js relations
  accounts Account[]
  sessions Session[]

  @@map("users")
}

model UserEmail {
  id String @id @default(uuid())

  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  email      String    @unique
  type       EmailType @default(PERSONAL)
  isPrimary  Boolean   @default(false)
  isVerified Boolean   @default(false)
  verifiedAt DateTime?

  // opcional: amarrar diretamente ao tenant (útil quando é corporativo)
  organizationId String?
  organization   Organization? @relation(fields: [organizationId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
  @@index([organizationId])
  @@map("user_emails")
}

enum EmailType {
  PERSONAL
  CORPORATE
}

model Organization {
  id           String  @id @default(uuid())
  name         String
  slug         String  @unique // será usado na URL
  domain       String? @unique // opcional para validar o e-mail corporativo
  processLabel String  @default("Processo")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  processes    Process[]
  emails       UserEmail[]
  departments  Department[]
  ProcessClass ProcessClass[]

  @@index([name])
  @@map("organizations")
}

model Department {
  id   String @id @default(uuid())
  code String
  name String

  // tenant
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  // hierarchy
  parentId String?
  parent   Department?  @relation("DepartmentHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children Department[] @relation("DepartmentHierarchy")

  // opcional: caminho materializado (útil no futuro p/ consultas de descendentes)
  path String?

  //  vínculos
  memberships DepartmentMembership[]
  processes   Process[] // Processos atualmente associados a este departamento

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([organizationId, code]) // sigla única por org
  @@unique([organizationId, name]) // nome único por org
  @@index([organizationId])
  @@index([parentId])
  @@index([path])
  @@map("departments")
}

model DepartmentMembership {
  id String @id @default(uuid())

  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  departmentId String
  department   Department @relation(fields: [departmentId], references: [id], onDelete: Cascade)

  role     DepartmentRole @default(MEMBER) // papel dentro do setor
  isActive Boolean        @default(false) // para aprovação 

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([userId, departmentId]) // vínculo por usuário-setor
  @@index([departmentId])
  @@index([userId])
  @@map("department_membership")
}

enum DepartmentRole {
  ADMIN
  MANAGER
  MEMBER
  REQUESTER
}

model ProcessClass {
  id             String       @id @default(uuid())
  // escopo (tenant)
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  // identificação
  code        String // ex: 'Acesso ao sistema'
  name        String // 'Solicitação de acesso ao sistema'
  description String?

  // Hierarquia (para agrupar classes no futuro)
  parentId String?
  parent   ProcessClass?  @relation("ProcessClassHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children ProcessClass[] @relation("ProcessClassHierarchy")

  // metadados úteis
  isActive Boolean @default(true)
  slaDays  Int?

  // relacionamentos
  processes Process[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([organizationId, code])
  @@unique([organizationId, name])
  @@index([organizationId])
  @@index([parentId])
  @@map("process_classes")
}

model Process {
  id          String        @id @default(uuid())
  title       String
  description String?
  status      ProcessStatus @default(OPEN)

  // classe do processo (obrigatória)
  classId String
  class   ProcessClass @relation(fields: [classId], references: [id], onDelete: Restrict)

  // tenant
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  // departamento ao qual o processo está associado ou disponível
  departmentId String?
  department   Department? @relation(fields: [departmentId], references: [id])

  // dono ou quem abriu o processo
  ownerId String
  owner   User   @relation("ProcessOwner", fields: [ownerId], references: [id], onDelete: Restrict)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([organizationId])
  @@index([departmentId])
  @@index([ownerId])
  @@index([status])
  @@map("processes")
}

enum ProcessStatus {
  OPEN
  IN_PROGRESS
  CLOSED
}

// Auth.js (adapter + flows)

model Account {
  id                String  @id @default(uuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

model Session {
  id           String   @id @default(uuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

// Token para reset de senha (validade: 30 minutos via app)
model PasswordResetToken {
  id         String   @id @default(uuid())
  identifier String // email
  token      String   @unique
  expires    DateTime

  createdAt DateTime @default(now())

  @@map("password_reset_tokens")
}
